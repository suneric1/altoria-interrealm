using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Altoria {

	public enum ItemType {Default = 0, Mask, Weapon};
	public enum PatternType {SunMoon = 0, TimeSpace, War};

}
